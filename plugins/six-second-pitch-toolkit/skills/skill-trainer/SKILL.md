---
version: 1.30.0
last_updated: 2026-08-24
status: active
audience: internal
name: skill-trainer
description: Dee's review + lessons-into-skills loop. After any work it runs an objective EVALUATE -> GAPS -> CLARIFY -> IMPROVE review of what was just done, through an ISO 9001 / Six Sigma lens (version control, single source of truth, doc sprawl, dates, repeated mistakes, false assumptions, which skills should have fired) AND folds the approved lessons back into the relevant skills so the team gets better over time. Fire it without waiting to be asked whenever the user reflects on how work went or how to improve, or says any of: "lessons learned", "what did we learn", "review", "retro", "retrospective", "process review", "what worked", "what didn't", "train the skills", or at the end of a major deliverable or session. Not for code correctness or pull-request review; it does not BUILD new skills (that is skill-creator) - it TRAINS the existing skills and the way of working.
when_to_use: After a deliverable, milestone, or session, to grade the WORK and the WAY OF WORKING and turn lessons into fixes that get folded back into the skills. Pair with the repo standards it cites; this skill RUNS the review and trains the skills, the standards GOVERN how skills are written and shipped.
---

# Skill Trainer — Dee's review + lessons-into-skills loop (was "Process Review")

Run this when Dee wants an honest look back at what was just done and how to make both the work AND the way of working better. It replaces the long prompt she used to paste. Treat it as a quality system, not a pep talk: the goal is fewer repeated mistakes, a cleaner paper trail, and a process a 6th grader could follow.

**How this fits the repo:** the `SKILL-AUTHORING-STANDARD.md` and `OUTPUT-QA-AND-DELIVERY-STANDARD.md` (repo root) are the *rulebooks* for how skills are written and shipped. This skill is the *action* that audits a session or deliverable against that same ISO/Six-Sigma thinking. When a review turns up a skill that breaks a standard, fix the skill and bump it per that standard.

## First: get on the strongest setting
This is judgment work, so quality scales with model strength. Recommend Dee run it on the most capable model / highest reasoning effort available (her original prompt said "switch to Opus"). If you cannot switch, say so plainly and proceed.

## The one rule that makes this work
Do the four steps **in order**. **Do not skip a step. Do not combine GAPS and IMPROVE** by quietly fixing things. Name what is wrong first, ask before revising, and only fix after the user answers. Quietly fixing hides the lesson, so the same mistake returns later. This guardrail is the whole point.

## Step 1 — EVALUATE (what actually happened)
Assess what was produced against the **stated goal**. Be specific: mark each part **strong / adequate / weak** and say WHY. Ground it in the real thread, not memory:
- The feedback and criticism the user gave (quote the turning points).
- The mistakes and **false assumptions** made along the way.
- How the output got corrected, and what was finally **approved or praised**.

**Measure before you opine.** If a fact is checkable (which branch is live, what files exist, the real date, what's deployed, whether work was pushed), check it with tools first. A review built on memory repeats the errors it is supposed to catch.

## Step 2 — GAPS (no softening)
State plainly what is missing, underdeveloped, wrong, or risky. No hedging. If something is off, name it. **Rank by impact** (Pareto: the vital few first, not a flat list).

### Mandatory process checklist (inspect EVERY time)
Walk this out loud. These are the defects that repeat across sessions:

- [ ] **Single source of truth** — is the work built on the CURRENT doc, or a stale/superseded one? (Newest date is NOT automatically the truth; check for a STATE file that says what's authoritative.) **A handoff/session-notes claim ("X is still not done," "cancel Y") is a hypothesis, not a fact — search for evidence (e.g. `find`/`grep` the project tree) before repeating it to the user or acting on it.** (Added 2026-07-01 after a handoff's "cancel Mailchimp" and "GHL CSV not exported yet" both turned out stale — the user doesn't have Mailchimp, and the CSV was already saved from a prior thread. Both surfaced as if current instead of being checked first. See [[verify-before-assert]].)
- [ ] **Version control** — branch hygiene: merged/dead branches pruned; nothing valuable stranded on an abandoned branch; the working branch is the right one.
- [ ] **Saving / backup** — is the work committed AND pushed? Anything living only on local disk (data-loss risk)? **MEASURE this with `git status` / `git log` — never assert commit/push state from memory or assumption** (added 2026-07-01 after "nothing has been committed yet" was told to Dee when a parallel process had already committed most of it). **Watch for concurrent writers:** in a shared repo, another thread or an automated import may be committing at the same time — check `git log` timestamps, and **stage your own files selectively** (`git add <file>`) so you don't commit another thread's changes under your message or get your work swept into theirs with a misleading label.
  - ⚠️ **This rule failed inside a skill-trainer review itself, same day it was being checked (2026-08-20).** A GCPD/coalition review ran `git add Austin-Coalition/` to stage one PDF it had just filed, and the directory add swept in a parallel thread's 1,986-line research export that had landed seconds earlier, under a commit message that only described the PDF. No content was lost or corrupted (the swept file was itself a properly-marked EXTERNAL doc), but the commit history now misattributes it. **Logged as a rule-adherence failure, not a wording gap** — the rule already said exactly what to do. The tell to watch for: any `git add <directory>/` on a shared project folder, even one you believe you alone are touching, is the same shape of risk as `git add -A`. Name every file explicitly when a parallel session could plausibly be writing to the same folder — which in this workspace is close to always.
- [ ] **Any shared, non-git external folder gets the SAME check before mutating it** (generalized 2026-08-09; was git-only). Dropbox and Google Drive are mutable shared state too — Dee runs parallel threads against them exactly like a repo, but they have no `git status` equivalent to catch a race. **Before renaming/moving/deleting files in a synced folder, `ls` the current state first — do not act on a remembered or previously-listed file list.** Live case: a cleanup `mv` targeted 12 files by name from an earlier listing; a parallel thread had already reorganized the same folder, and every `mv` failed with "No such file or directory." No data was lost (the failure was loud), but a differently-shaped race (e.g. a blind overwrite instead of a rename) could silently clobber another thread's work. The fix is procedural, not tooling: `ls`/`find` the actual current folder content immediately before the mutating step, every time, the same discipline as `git status` before a commit.
- [ ] **Organization** — doc sprawl: are superseded files marked or archived? Can a newcomer find "what's current" in 30 seconds? **Before hand-building ANY new deliverable — a doc, OR a design asset/component/template — search the target folder for an existing one on the same topic first** — a plausible-sounding gap ("X isn't built yet") can be wrong, and a duplicate can omit a real deadline, spec, or design decision the existing one already had. (Added 2026-07-01 after a redundant Meet-With-Dee rebuild handoff was written before checking Dee Website for the existing, more detailed one, which also carried a hard deadline the duplicate missed. Generalized 2026-07-01 after the SAME failure hit a design asset, not just a doc: a locked, Council-approved acronym-card template already existed in `Dee Website/article-templates/` while a plain hand-coded box shipped instead on the "Is It a Sin to Use AI" article.)
- [ ] **Date integrity** — were files/commits stamped with the REAL date? (Verify with the `date` command.) **Re-run `date` IMMEDIATELY BEFORE writing any dated filename or dated log entry — not just at the start of a work stretch** (sharpened 2026-08-07): on the your author imprint project, `date` was run once (07-29), the session then sat idle for days, and the SAME conversation resumed on 08-04 — two deploy-log entries and two CANON lock dates got stamped "2026-07-29" for work whose commits are timestamped 08-04. A `date` check at the top of a stretch does not cover the bottom of it. The moment your fingers type a YYYY-MM-DD, that is when `date` runs. **And MEASURE per-file timestamps before renaming anything** (added 2026-07-29): when a session spans midnight, some files are legitimately dated the earlier day, so a blanket rename introduces NEW errors while fixing old ones. On the your author imprint session a blanket "fix" would have put 5 wrong dates on snapshots that were correctly dated, to correct 4 that were wrong. Run `stat -f '%SB' -t '%Y-%m-%d %H:%M' <file>` and `git log --date=format:'%Y-%m-%d %H:%M'` first, then rename only what the timestamps say is actually wrong.
  - **The turn-start hook had a real gap: it didn't cover mid-turn drift, and that gap is now closed with a second hook.** The SessionStart/UserPromptSubmit hook (in `~/.claude/settings.json`) verifies the date at the top of a turn — that part was already a real mechanism, not prose. The gap: a single turn can run for days (background renders, several skill calls in a row) with no new user prompt to re-fire that hook, so a date read once near the top gets silently reused at the bottom. This exact gap produced a real 5-day-stale commit on 2026-08-13 (2026-08-04 written when the true date was 08-09), inside a session that had already self-corrected the identical error twice earlier the same day. **Fix, built and proven the same session:** a second hook, `PreToolUse` on `Bash`, filtered to commands containing "git commit", that runs `date` fresh and injects it as context immediately before the commit executes — so the check happens at the moment of the write, not at the top of the turn. Tested with a synthetic stdin payload (commit command → fires; non-commit command → silent) and then proven against a real triggered Bash call. Before adding another prose sentence to this bullet for a future repeat, check `~/.claude/settings.json` first — the answer may already be "extend the existing hook to more write sites (e.g. `Write`/`Edit` on dated filenames)," not "explain it better."
- [ ] 🔴 **BANNED WORDS: "fixed" / "done" / "shipped" / "live", unless the SAME message shows what the LIVE url returns RIGHT NOW** (added v1.24.0, 2026-08-20, Dee's call). This replaces the prose version below with something that produces output, because the prose version failed twice in one day.
      **The failure:** on 2026-08-20 a session fixed three real live defects on faithfocushaven.com (a 404ing email-capture script on the homepage and /tools/, a misattributed research citation on TWO public pages, and a hero line that aimed the joke at the reader), reported all three, and **none of them were deployed.** The `--branch main` command was blocked, so the public kept seeing every defect. The checklist item directly below *already said to check this* and did not fire. It only surfaced because a SECOND review forced the measurement.
      **How to run it.** Before the words fixed/done/shipped/live appear in a message about anything with a public surface, run the check and paste the result:
      ```bash
      curl -sL -o /tmp/live.html -w '%{http_code} %{size_download}b\n' "https://THE-REAL-URL?v=$(date +%s)"
      grep -c 'THE-THING-YOU-FIXED' /tmp/live.html     # 0 = the fix is live, 1+ = still broken
      ```
      **If it is not deployed, the required phrasing is: "fixed in the repo, still broken live."** Not "fixed." Those are different claims and only one of them is true for a user. Pair with a control (a nonsense URL) on any host that can soft-404. Same family as the taste gate in premium-ux v1.49.0 and the date hook: **give the judgment an artifact or it will not fire.**
- [ ] **Is the broken version still live?** (added 2026-07-28) When this session FIXED something that exists on a public surface, check what that surface serves *right now* (`curl` the live page/README/file) and say it out loud: "the fix is committed locally, the broken version is still up." A local fix plus an intentional hold is a legitimate choice, but it must be a *stated* choice with a known cost, not a quiet gap nobody names. Why: during the toolkit install relaunch, the crisis-causing "paste these 2 commands" README stayed live to the public for the entire session while a corrected version sat committed-unpushed, and no one said so until the review measured it. Sibling of [[verify-before-assert]]: "we fixed it" and "users see the fix" are different claims.
- [ ] **A decision touched by TWO projects' docs can get "Locked ✅" in each, disagreeing, on the SAME day** (added 2026-08-08). When a fact belongs to more than one project (a domain, a shared brand line, a cross-repo name), each project's SSOT can independently record a session's conversation with Dee and lock opposite conclusions, both citing "Dee's call" with the same date. Neither thread checked the other's file. **Live case:** on 2026-08-04, `your author imprint/CANON.md` locked "consumer domain = tinaandtilly.com" while root `Documents/Claude/LINKS.md` already held the opposite, locked 2026-07-29 and reconfirmed 08-04/07 ("Dee locked redirect-only instead"). Both said "Locked ✅." It sat wrong for 4 days and nearly caused a live redirect to be reversed on the strength of the wrong file. **How to run it:** before calling anything "Locked ✅" that touches a domain, a shared name, or a cross-project fact, grep the OTHER project(s) that reference the same thing for a conflicting lock. If the ONLY evidence for a lock is one project's own file, that is not enough when a sibling project's file exists and disagrees.
- [ ] **LOCKED does not mean PROPAGATED — verify every locked decision is actually live, and record where you checked** (added 2026-07-29, after the SAME defect surfaced twice in one hour). A decision marked `Locked ✅` in a canon/decisions file is a statement of INTENT. It is not evidence that any surface obeys it. On the your author imprint project: **GIRLS** was locked 2026-07-05 (5 letters, I=Imaginative, S=Strong, declared-not-becoming) and the live site still served the old 4-letter version 3+ weeks later; and `faithfocuskids.com → 301 → hub` was marked `Locked ✅` while the domain sat parked at the registrar with no redirect and no valid SSL. **How to run it:** for each locked row touched by this work, list the surfaces it claims (live site, DNS, README, listing, profile field, printed file), CHECK them with a tool (`curl`, `dig`, grep), and write the result back next to the decision. A locked decision with no propagation check is an unverified claim, and it is the single most repeated defect in this project's history.
- [ ] **A third party's own published access claims are a claim, not evidence of access — verify by attending, not by reading** (added 2026-08-20). This generalizes the LOCKED-vs-PROPAGATED lesson above beyond the user's own canon files to any external institution's published word. **Live case:** a state agency's own meeting agenda said "Public Comment, 3 minutes per person," and its meetings page said a "live audiovisual stream" was available with Zoom registration required. Both were reported to Dee as verified facts, sourced correctly from the agency's own documents. **Neither was actually true on the day** — Dee attended and there was no public comment and no live A/V. The documents were not lies exactly; they described what the agency intended or usually does, not what it delivered that day. **How to run it:** when a claim of the form "the public can do X" or "X will be available" comes from an institution's own published material rather than a firsthand account of it happening, flag it as *stated, not confirmed* until someone in the room says otherwise. Don't let a well-sourced citation read as verified reality — the citation only proves what was published, never what occurred.
- [ ] **Secrets** — did any API key, password, or token get pasted into chat? If so, flag it for rotation. **And an open USER-action item does not survive as a footnote — give it a dated ACTION file and the TOP of the message** (added 2026-08-07): the your author imprint GitHub-token rotation was flagged 07-29 and then re-mentioned at the BOTTOM of 4 long messages across 9 days; it never got done, and a scan then found the token embedded in the remotes of ALL 26 repos, not 2. What finally made it actionable: (a) measure the real blast radius (scan every repo, don't assume), (b) write a dated `ACTION-*.md` at the workspace root with paste-ready commands and a one-line 'delete this file when done' completion signal, (c) script the bulk part so the user's share is one login + one paste, (d) put the item at the TOP of the next message, and keep it there each session until the action file is gone. A nag that lives in closing-paragraph prose is a nag the reader has already learned to skip.
  - ⚠️ **The leak is usually a COMMAND, not a paste (added 2026-08-19).** The bullet above assumes a secret arrives by being pasted. In practice it arrives because *you ran something that printed it*. `git remote -v` prints the full remote URL, and Dee's repos carry the token inline (`https://ghp_xxx@github.com/...`), so a routine push-state check dumps a live credential into the transcript. It happened this session during skill-trainer's OWN measure-first step, in a workspace whose memory says never print that value. **How to run it: never print a raw remote.** Use `git remote get-url origin | sed -E 's#//[^@]*@#//#'`, or pipe any command that may surface a remote through `sed -E 's#ghp_[A-Za-z0-9]+#[REDACTED]#g'` — including `git push` output. Applies to `git remote -v`, `git config --list`, `git log` on URL-bearing notes, and `cat .git/config`. This is a mechanism, not a reminder: the filter goes in the command, so it cannot be forgotten 40 turns later.
    - 🔴 **IT HAPPENED AGAIN 2026-08-24, in the FIRST FIVE MINUTES, again inside skill-trainer's own
      measure-first step. 2-for-2, so it is now a HOOK, not a sentence.** `git remote -v` across
      3 repos printed the live PAT twice in one call. The rule above was 5 days old and specific,
      and did not fire — a prose rule only fires if the model re-reads it at the moment of action.
      **Built + tested:** `~/.claude/qc/secret-print-gate.sh`, `PreToolUse` on `Bash`, warns rather
      than blocks. **It false-positived TWICE on its own author** (a heredoc documenting the rule,
      then the git commit message describing the fix), so v2 matches only at a real command position
      and guards heredocs/commit messages/echo. 7-case regression suite passes.
      **Two corrections worth keeping:** (1) `PreToolUse` hooks do **not** wait for session start —
      this one fired on the next Bash call, contradicting the session-start claim in `CLAUDE.md`;
      (2) a string-matching gate must distinguish RUNNING a command from WRITING ABOUT one, or the
      person writing the rule down is the first one it punishes.
      **Before adding a third wording here, check whether this hook fired and why not.**
    - ✅ **SECOND CONFIRMED INSTANCE, and the mechanism works when applied (2026-08-24).** A 2026-08-18 session ran a bare `git remote -v` on the command-center repo during an ordinary "where are we" check — not inside a skill-trainer run — and **printed the live `ghp_` token into the transcript**, one day before this rule was written. So the leak surface is broader than this bullet's original framing: it is not specific to skill-trainer's measure-first step, it is *any* orientation check on a repo whose remote carries an inline token. On 2026-08-24 the same check was re-run with `git remote get-url origin | sed -E 's#//[^@]*@#//#'` and returned a clean URL. **Keep the sed in the command; do not rely on remembering which repos have inline tokens.** Note also that Dee **declined rotation on 2026-08-18 and asked not to be reminded** — that decision stands and is separate from this. Not printing it is Claude's job and requires nothing from her.
- [ ] **A bulk status/link check needs a KNOWN-GOOD CONTROL in the same batch, and must never run against a host you just crawled** (added 2026-08-19). If a whole host comes back dead, suspect the instrument before the finding. **Live case:** a check of 243 documents returned **204 "ERR" results, all on `gov.texas.gov`** — every one false. Cause: 73 pages crawled from that host, immediately followed by 243 parallel HEAD requests at `-P 20`. The host stopped answering. The tell was that a PDF downloaded successfully 20 minutes earlier appeared in the "dead" list; the confirmation was that the same host served a fresh file fine 40 minutes later.   - ✅ **A MECHANISM NOW EXISTS FOR THIS (built and tested 2026-08-23, Dee's call).**
      `~/.claude/qc/diagnosis-control-gate.sh`, wired as `PreToolUse` on `Write|Edit`. It fires when
      a pending write **states a cause** ("is blocking", "the reason is", "root cause", "at the
      server level", "does not support") and reports how many control/measurement markers the same
      write contains. It forces 5 questions: did I measure or infer · **where is my control, in the
      same batch seconds apart** · could the instrument be the cause · does her own repo already
      answer this · if I cannot answer the first two, write it as UNVERIFIED.
      **Built after a 2026-08-23 session produced 11 wrong assertions in one day**, most checkable
      with one command. The worst reported your host as blocking AI crawlers, wrote it into a repo
      doc as a finding, and sent Dee into Site Tools hunting a setting that does not exist. The real
      cause was Claude's own IP being rate-limit banned by hammering the host. One control host
      disproved it. **The rule below already said exactly this and did not fire**, which is why it
      got a hook instead of a re-wording.
      ⚠️ **Do not add prose here for a future repeat. Check whether the hook fired and why not.**

**Why it matters here specifically:** in this workspace a dead-link finding is a *deliverable* — it goes to state agencies and into public comment. A fabricated mass-rot claim is worse than no finding. **How to run it:** low parallelism, generous timeouts, GET-with-redirects rather than bare HEAD (some hosts refuse HEAD), a control URL in every batch, and a cool-off before re-checking a host you crawled. Same family as the Wayback "0 recoverable" and binary-grep false negatives already logged in that project's STATE.md.
- [ ] 🔴 **A SUMMARIZING FETCH CAN SILENTLY DROP A WHOLE TABLE. Anything schedule-shaped gets the browser, not WebFetch** (added 2026-08-24). New instrument failure, same family as binary-grep and the Wayback 429, and it cost a full wrong answer to Dee twice in one session.
      **Live case:** asked to track the Texas House live-broadcast schedule, WebFetch was pointed at `house.texas.gov/videos` and at `/video-audio` and reported both times that the page was **"primarily a video archive, not a schedule… No upcoming broadcasts with specific dates and times are listed."** Dee was told, on that basis, that no broadcast schedule existed. It was wrong. Opening the same URL with the browser tool showed a full **"Live Broadcasts / Broadcast Schedules"** table — Start Time, Date, Event, **Capitol Channel number**, Room, Notes — with ~25 dated rows running weeks out. The HTML-to-markdown conversion had eaten the `<table>` and the summarizer then truthfully reported what it had been handed. **A third WebFetch, asked point-blank for the link's href, confidently returned the wrong URL** — the redirect target rather than the page holding the table.
      **Why it is dangerous:** the failure produces a *confident negative*. "There is no schedule here" reads like a finding, not an instrument error, so nothing prompts a re-check. Compare the bulk-link-check rule above: a whole host coming back dead should make you suspect the instrument. So should a whole page coming back empty.
      **How to run it:** if the thing you are looking for is a **table, schedule, calendar, roster, fee chart, or docket**, use the browser (`preview_start` + `get_page_text`), not WebFetch. Use WebFetch for prose. And when a fetch returns "this page does not contain X" for an X the page is *named after*, that is a re-check trigger, not an answer.
- [ ] **A tracked fact has a SHELF LIFE. A recurring event's next occurrence, and a "Verified" date, both decay** (added 2026-08-24). Two shapes of the same defect, one session apart.
      **Shape 1, the recurring event.** A monthly advocacy call was captured on 2026-08-17 as "4th Tuesday, 2pm, next Aug 25" and written into Notion, `NEXT.md`, and the `.ics` with an alarm. On 2026-08-23 Dee was told, in a "what's next" summary, to sign up for it. **Dee then sent a screenshot of the organization's own page reading "Aug 25, CANCELLED."** The row was never wrong when written; it went stale, and nothing re-checked it because a recurring event *feels* like a standing fact rather than a claim. **The alarm would have fired the next day telling her to attend a cancelled call.**
      **Shape 2, the decaying control.** The same tracker carries a **`Verified` column** whose entire purpose is to stop anyone trusting a stale row — its own description says "Do not trust a row whose Verified date is more than a few weeks old." On 2026-08-24, **every one of the 6 open rows still read `Verified: 2026-08-17`**, a week untouched, including two hearings 3 weeks out that had never been re-pulled. Dee's own root `CLAUDE.md` already names this failure mode in one line: *"A control nobody maintains is worse than no control, because people trust it."*
      **How to run it:** before repeating ANY dated item back to Dee as actionable — especially in a "what's next" summary — re-pull the source for anything whose `Verified` date is older than ~7 days, and treat every recurring event's *next* occurrence as unverified until the organizer's own page is checked that week. Re-stamp `Verified` when you check. An unre-checked row is a hypothesis wearing a date.
- [ ] **Two documents going to the SAME recipient on the SAME day must not share their opening and closing lines — and you have to MEASURE that, not assume it** (added 2026-08-24). **Live case:** two electronic public comments were drafted for the same House committee, same hearing, same day, under Dee's name. The middles were genuinely different (different charge, different facts, different ask). The **first and last sentences were word-for-word identical in both** — "I am the parent of a Texas public school student." / "Thank you for the chance to comment." To a staffer opening both, that is the tell of a form letter, and it discounts both. **Claude did not catch it. Dee did**, asking *"does it seem different enough that it doesn't look like the same as the first."* Only then was the overlap actually measured (a 3-line script comparing sentence sets), which found the two identical bookends immediately. **How to run it:** whenever a session produces more than one document aimed at the same reader, diff them against each other before delivering — compare first sentence, last sentence, and shared-sentence count — and vary the bookends deliberately. Assuming "the content is different so they read as different" is exactly the assumption that failed here.
- [ ] **Reading a project's SSOT means inheriting its METHODS, not just its FACTS** (added 2026-08-19). The SSOT checks above all test whether you got the *facts* right. They do not catch skipping a *procedure* the project already proved out. **Live case:** this session read `STATE.md` — including its record of a 2026-08-17 crawl that harvested 459 links across 5 seed pages and status-checked every one — then, handed a new agency page, read exactly one PDF and reported the analysis as done. Dee had to ask twice (*"did you download all the links... and catalog all this?"*) to get the established method applied. The facts were all correct; the method was ignored. **How to run it:** when you open a project's STATE/SESSION-NOTES and find a documented procedure for the kind of task in front of you, say out loud that it exists and either follow it or state why it does not apply. A prior session's method is a decision, not a memoir.
- [ ] **Attribution audit — did we write our own inference down as the USER'S decision?** (added 2026-07-30) Walk every doc this session produced and, for each factual claim about a person who is NOT in the conversation, name the source out loud: *the user said it*, *the third party said it*, *the user HEDGED it*, or *Claude inferred it*. Anything in the last two categories that got written as settled fact is a defect. **Three specific shapes to hunt:** (a) a synthesis of your own (a Council verdict, a research conclusion) later quoted back to the user as their idea; (b) a general guideline the user gave ("21 to 24 weeks is normal") converted into a specific commitment ("Tue Jan 5"); (c) a hedge ("I *think* both books are about X") hardened into a premise that whole documents get built on. **Why:** on 2026-07-28/29 all three shapes happened in one session, 5 instances total, and 3 of them occurred AFTER a memory had already been written about it. Dee had to say "when did I say they're launching as a cohort?", "I dont know where you got michelle's launch date. she said september", and "you have to also understand that i THINK thats what their books are about." A prose memory did not stop it because it required remembering a rule 40 turns later while writing a table. **The fix that works is a SOURCE COLUMN:** any doc recording facts about a third party carries the origin inline next to each fact. You cannot write "Jan 5" into a table whose next cell must say who said it. See `Book Launch Lab/TIMING.md` for the pattern that finally held.
- [ ] **Did a rule that ALREADY EXISTS fail to fire? That is a bigger finding than a new lesson** (added 2026-08-08). Before writing a new rule, grep this skill and the relevant skill's CHANGELOG for the lesson you are about to add. If it is already there and the session broke it anyway, do NOT re-add it in different words. Report it as a *rule-adherence* failure and ask what would make it fire, because a second copy of a rule that did not work the first time is noise. **Live case:** skill-trainer v1.11.0 (2026-08-07) had just added BOTH "run `date` at the moment of writing a dated entry" AND "an open USER-action item never survives as a footnote." The very next session broke both: it stamped 08-04 on a file created 08-05, and it named 4 stale public surfaces in chat while putting none of them in the user's tracker. The rules were 1 day old. **What this means:** a prose rule inside a skill only fires if the model re-reads it at the exact moment of the action. Rules that must survive 40 turns need a MECHANISM (a required field, a scripted check, a tracker write), not better wording. When you find this pattern, say so plainly and propose the mechanism.
- [ ] **Every follow-up you NAME goes into the user's tracker before the session ends** (added 2026-08-08). Naming a leftover in chat, a session note, or a deploy log is not tracking it. Chat evaporates; a deploy log is read only by whoever opens that file. **Live case:** this session identified 4 surfaces still teaching a superseded install path, reported them in a message, and put zero of them in the Asana board it had built that same session for exactly this purpose. One of them (a wrong "you need a paid plan" claim on a public README) then stayed live 4 more days. **How to run it:** at the end of GAPS, list every "still to do / not fixed / worth doing later" item you produced, and write each one into the tracker (or state out loud that the user declined). **And measure the blast radius before you write the task** — the same session wrote "2 exposed tokens" into a task when a real scan showed 27 repos.
  - ⚠️ **THIS RULE FAILED AGAIN 1 DAY AFTER IT WAS WRITTEN (2026-08-09), which makes it a mechanism problem, not a wording problem.** The T&T Bestseller-Launch session named ~10 follow-ups inside a repo doc and wrote exactly ONE into a tracker. The most time-critical of them — a a paid coaching program class 5 days out — lived in 3 markdown files and 0 trackers. **It got worse than "untracked":** because nobody opened the calendar, nobody noticed the class *collides* with a non-drop-off school event for the user's daughter. A follow-up that only lives in a repo doc cannot collide-check against real life. **The mechanism that works (proven on the date rule, v1.14.0): make it a tool step, not a sentence.** The tracker is the user's CALENDAR (Dee's answer, 2026-08-09) — so the required step is: *open the calendar for the dates you just named, look for conflicts, then write the items in.* Reading the calendar is what catches the collision; writing to it is what survives the session. **Also re-read the existing tracker entries you find there** — in the same session, two calendar reminders were carrying a domain fact that had been reversed a day earlier, so the tracker itself was propagating a superseded decision.
- [ ] **An outside AI's finding about the user's own material gets VERIFIED before it reaches her, not after** (added 2026-08-09). Dee runs ChatGPT/Perplexity threads in parallel and brings the output back for evaluation. Those threads confidently critique her locked content while working from a stale mirror. **Live case:** a ChatGPT audit ruled that T&T Challenge 30 must be REPLACED because telling gray thoughts to "go away" suppresses feelings. It was reported to Dee as "a real catch" — and it was wrong. Her own 8/03 voice notes (~21:26, ~33:56) show the exercise *already* names the feeling first ("I want to name the feelings up top... and I tested with Jolie and she liked it"). The audit had also missed that The Storm Cloud was locked the day before. Endorsing it nearly replaced tested-with-the-actual-kid IP. **How to run it:** before repeating ANY external-AI critique of the user's locked content, grep her own source (voice-note transcripts, CANON, the dated decision) for the thing being criticized. Quote what you find. If you cannot verify it in her material, present it as "this AI claims X, I have not verified it" — never as a catch. The existing "don't take other agents at face value" instinct is not enough here, because an external thread reads as *the user's own work* when she is the one who pasted it.
- [ ] **EDITING existing brand copy triggers the read-the-SSOT-first rule exactly like WRITING new copy does (added 2026-08-14).** `ama-research-engine` Step 0 ("show the receipts") and the SSOT rule above both read as though they fire when you *author* something. They do not fire on their own when the task feels like *editing* — pulling two paragraphs out of an existing article, swapping a phrase the user asked to change, tightening a line. That framing is what let a session quote and rewrite article passages containing a **retired reader age** without ever opening the project's `CANON.md`, which had locked the correct age the day before and calls that exact field "the single most repeated defect in this project's history." Dee caught it, not the gate. **How to run it:** before touching ANY line of existing brand copy, open that project's `CANON.md` / `NAME-REGISTRY.md` / `STATE.md` and paste the lines governing every factual field the copy touches (ages, names, hierarchy, product descriptions, dates), even when you are "only" editing one sentence. A passage you are about to edit is a passage you are about to re-endorse.
- [ ] 🔴 **A PROPOSAL document is not an OUTCOME document, and never "confirm" a figure with arithmetic that makes it match what the user remembered** (added 2026-08-20). Two rules already in this file should have caught this and neither fired, so it is logged as an adherence failure **with a mechanism**, not as new wording.
      **What happened.** Dee said her Texas Housing Trust Fund was "the largest in funding history." A session found `$24,660,768/yr` in an **LBB conference committee ISSUE DOCKET** and reported the fund at **$49.3M**. An issue docket shows what **each chamber proposed**; the Senate column was $8.16M/yr and **the House number never prevailed.** The enacted figure, stated in one plain sentence in the **LBB Fiscal Size-Up 2010-11 p.437**, was **$21.9M** — which is exactly what Dee had said from memory. The session had even written "I could not find a document labeled enacted" as a caveat, then used the number anyway, which made the caveat decorative.
      **The second, worse half.** It then "corroborated" the error: took the House figure, subtracted the 10% admin withholding, got $22.19M, and matched that to Dee's "22M." **Dee had written "22M *either* annually"** — "either" was her flagging uncertainty — and the session resolved her hedge toward its own number instead of asking. That is **Attribution Audit shape (c)** verbatim, a rule added 2026-07-30.
      **This is the LOCKED-vs-PROPAGATED family** (2 bullets up) in a new domain: a bill, a chamber mark, a budget request, a docket, an agenda, or an AI-drafted resume all state **intent**, never outcome.
      - ✅ **EXTENDED 2026-09-01 to cover ORIGIN / LAUNCH / DATE claims, after this exact family recurred on a non-money surface.** A session researching Six Second Pitch's history found a 2014 archived landing page with three separate marketing lines: "The Six Second Pitch", "Coming to SXSW 2014", and "Launching March 7, 2014". It merged two of them into "created it in 2014 and launched it at SXSW on March 7, 2014" and **published that to yourbrand.com/frameworks AND about.me**, including into JSON-LD as `dateCreated`. Dee corrected it from memory: *"I dont thinkI launched it at sXWw at all."* She was right. A marketing page saying "Coming to X" states a PLAN; a separate line saying "Launching DATE" may describe a different thing entirely. **The prose rule in this bullet already covered "an agenda, a draft" and did not fire, and the hook could not fire because there was no dollar figure and no superlative.** So the hook's scope was widened rather than the prose re-worded: it now also counts origin/launch/date claims, and its question 2 explicitly names marketing/landing/announcement pages as INTENT documents and forbids merging two lines into one claim. Regression-tested 9/9 (fires on "launched at SXSW on March 7, 2014", "founded in 2011"; silent on the safe rewrite "has been teaching it since 2014", and on a bare year with no claim). **Two lines that sit next to each other are not one sentence. Quote them separately or verify they describe the same event.**

      - 🔴 **A SECOND GATE EXISTS NOW: FABRICATED QUOTES (built + tested 2026-09-01).** The extension logged directly above was shipped, and then **the very next stretch of the same session produced a different failure the extended gate could not see**: three quotes attributed to Dee that she never said, from two different AIs, on copy headed for a printed magazine. ChatGPT invented *"Sometimes we don't need another 20-step plan," Patience says* and a second interior-thought quote; **Claude then carried the first one into its own draft as the PULL QUOTE.** A grep of her canon found it nowhere. Dee spotted the pattern, not the tooling. **The extended figure-source-gate was MEASURED against all three and stayed silent on every one** - no money figure, no superlative, no date - which is exactly why this got its own gate rather than another sentence: `~/.claude/qc/quote-attribution-gate.sh`, `PreToolUse` on `Write|Edit`, regression-tested 13/13. **Two lessons worth more than the gate itself:** (1) a false quote is a WORSE category than a wrong figure, because it goes to print under a real person's name and cannot be unpublished; (2) **authentic is not the same as on-message** - in the same session Dee rejected a quote that genuinely WAS hers, from her own published Substack, because it predated her current positioning. Verifying the source is necessary and not sufficient; ask whether it represents her now. See memory `dee-quotes-must-be-sourced-never-composed`.

      **The mechanism (built and tested 2026-08-20):** `~/.claude/qc/figure-source-gate.sh`, wired as `PreToolUse` on `Write|Edit` in `settings.json`. It scans the pending write for money figures and record/superlative claims and, when it finds them, forces 4 questions before the write lands: which document, is it final or a proposal, did I derive this to make it match her, and did she hedge. Same shape as the git-commit date hook, which is the only fix in this file's history that ever stopped a repeat. **Before adding prose here for a future recurrence, check whether that hook fired and why not.**
      **And the standing lesson underneath it:** in this workspace **Dee's memory has beaten the document more than once.** When her recollection and your source disagree, the source is a suspect too. Say "I cannot verify that yet," never "your memory is wrong."
- [ ] 🔴 **A PASTE-READY doc carries ONLY what goes in the target. Notes to the user go somewhere else** (added 2026-08-25, Dee's approval). **Live case:** a session wrote `MOU-fill-in-sections-16-and-17.md`, whose stated purpose was "paste into the .docx," and put its 5 proposed priorities *directly beside* a warning addressed to Dee: *"Quoting the wrong one to people who have read it is an avoidable own-goal."* Dee pasted the block, **signed it, dated it, and sent it to the coalition organizer.** She was right to; it was labeled paste-ready. The defect was mixing two audiences in one block. **How to run it:** any file described as paste-ready, copy-this, ready-to-send, or drop-in gets a hard audience boundary. Content for the target document only. Guidance, caveats, "watch out for," and rationale go in the chat message or a separate clearly-named file. Before handing over such a file, reread it as the RECIPIENT of the final document, not as the user. If a sentence would confuse or insult that reader, it is in the wrong file.

- [ ] **A recommendation must name the specific thing it unlocks, or it is template-filling** (added 2026-08-25, Dee's approval). **Live case:** a capability statement has a "certifications" line, so a session recommended Dee call the state HUB program and get City MBE/WBE certification. She killed both in 5 words each: *"why would I contact HUB"* and *"WBE why would I get it when I'm ot doing anything."* Both were right. She had no bid in front of her, and the state program had been restructured to service-disabled veterans anyway. The recommendations existed because the FORMAT had a slot, not because she had a need. **How to run it:** before recommending any credential, registration, certification, tool, subscription, or account, write the sentence "this unlocks ___, which she is pursuing because ___." If you cannot fill both blanks with something from the actual conversation, do not recommend it. Report it instead as "available if you ever need it, here is the lead time."

- [ ] **Never refer to a thing by its identifier alone. Say what it IS** (sharpened 2026-08-25, Dee's approval; the plain-language rule below already existed and did not cover this shape). **Live case:** a session said "Section 16" roughly a dozen times across several messages, always meaning *the blank Initial Priorities space in Terry's MOU*, and never once said so. Dee replied: *"section 16 I dont know what that is, but it sounds like I can get certified. does it cost money?"* She reasonably read a document section as a certification program. **How to run it:** document sections, form numbers, program codes, statute cites, post IDs, and version numbers all get their plain meaning attached on EVERY use in conversation, not just the first: "Section 16, the blank priorities list in Terry's MOU." The existing rule below covers system IDs; this covers the more common case of a legitimate external identifier used as if the user shares your context.

- [ ] **A count you REPORT is a claim. Re-measure it at the moment you act on it** (added 2026-08-25, Dee's approval). **Live case:** a session counted 70 em dashes across 6 files in a shared folder, reported that number to Dee, and she approved cleaning them. When the cleaning script ran ~2 hours later it changed **zero** characters, because a parallel session had swapped every file in between. No harm done, but the number reached her as a fact and drove a decision. **How to run it:** any count that will drive a task (files to fix, links to check, instances to clean) gets re-measured in the same tool call that acts on it, and the before/after both get reported. If they disagree, say so out loud rather than quietly reporting success. Sibling of the control-in-every-batch rule above: in this workspace a shared folder has no `git status`, so the only defense is measuring at the moment of the write.

- [ ] **Repeated mistakes** — what did the user have to say more than once? Each repeat is a process defect, not a one-off.
- [ ] **Locked work untouched** — was anything finished/locked changed without being asked? (Change only what was requested.)
- [ ] **Right tools** — which skills/plugins SHOULD have been used and weren't? When a lesson is approved, fold it back into the relevant skill (and bump it per the authoring standard), not just this one deliverable.
- [ ] **Inconsistencies** — names, numbers, dates, or statuses that disagree across docs.
- [ ] **Plain language — including in THIS skill's own CLARIFY questions** (strengthened 2026-07-30). The Step 3 questions are the most jargon-prone thing in a review, because they are written straight out of the analysis. On 2026-07-30 a review flagged jargon as a defect and then asked Dee 4 questions containing "source column", "cadence check", "contraction density", and "a hook in settings.json". She answered 3 of 4 with "i dont understand those options" / "i dont know what this means". **Before asking, reread every option as the user: no internal terms, no tool names, no file paths, no metric names.** Say "make me show where every fact came from", not "add a source column". Say "count the everyday words like don't and you're", not "contraction density floor". A question the user cannot parse is a wasted round trip and it reads as being talked past.
- [ ] **Plain language** — did we speak the user's language, not the system's? Flag any internal IDs (post/attachment/page numbers, git hashes, version numbers) or DB/CMS/dev jargon used *in conversation* — refer to things by title + where they live. And when the user sounded lost/overwhelmed, did we zoom out to plain English proactively, or keep spiraling in detail? (Added 2026-07-01 after "fix post 864" + version-number talk lost Dee twice: "I have no idea what post you're talking about" / "what are you talking about and where are we?")

### The tracker step is a TOOL CALL, and it happens BEFORE the closing summary

> 🔴 **CORRECTED 2026-08-20 (Dee). Everything below used to say "write the follow-ups to the calendar."
> That is now WRONG and it was making her calendar unusable.** Her rule, given 2026-08-19: **only
> VERY URGENT items go on the calendar, and they go as all-day / FREE, never as timed-busy. Routine
> follow-ups go to the project's own tracker** (its `STATE.md` / `NEXT.md` / `TRACKER.md`) **or Notion.**
> A review that dumps ten housekeeping items onto her calendar has not tracked them, it has buried
> the two that mattered. **This skill taught the wrong thing for 6 days; a 2026-08-20 session hit the
> contradiction mid-run and followed Dee over the skill, which was correct.**

**Do this as the last action of GAPS, before you write one line of the closing summary:**
1. **READ the calendar** with the calendar tool for the dates you just named. Reading is non-negotiable
   and is the whole reason this is a tool call: **only the calendar can collide-check against real
   life.** A repo file cannot know she has a 9:00am policy hearing the morning you handed her a task.
2. **Sort what you found.** For each follow-up ask: *does this have a date, a deadline, or a
   consequence for slipping?*
   - **Urgent / dated** → write it to the **calendar**, `allDay: true`, `availability: FREE`, with the
     real context in the description (not just a link, per her standing rule).
   - **Everything else** → write it into the **project's own tracker file**, the one that project
     already uses. Do not invent a new doc; doc sprawl is a defect this same checklist flags.
3. **Search the calendar before you add** (`fullText`), so you do not duplicate an entry, and
   **re-read what is already there** - the tracker itself can be carrying a superseded decision.
4. Paste back what you wrote and **where**, or state plainly that the user declined.

**Live proof this works (2026-08-20):** a review searched the calendar for "IP assignment Jolie" and
got **zero results**, on an item the project handoff had called "the most overdue item in this folder"
for a week. It went to the calendar because it was genuinely urgent (a page had just gone live
publicly crediting her work while the assignment was unsigned). The other **9** items from the same
session went into `STATE.md`. That split is the rule working.

**A named follow-up with no entry ANYWHERE is an open finding in the review itself.** Count them,
name who wrote each one, and report the number.

### Why this rule looks over-engineered: it failed 5 times

**Kept as history, NOT as instructions. The only instructions are the 4 numbered steps above.**
The duplicated instruction block that used to sit here was removed 2026-08-20 because it still said
"write each named follow-up to the calendar," contradicting the corrected rule directly above it. A
skill that gives two different orders is worse than one that gives none.

The failure record, because the shape of it is the useful part:
- **2026-08-08** named 4 stale surfaces in chat, wrote 0 to the tracker. One stayed live 4 more days.
- **2026-08-09** named ~10 follow-ups in a repo doc, wrote 1. The most time-critical was a class 5
  days out that also *collided* with a family event, which only a calendar read could have caught.
- **2026-08-14** named ~5 leftovers and wrote 0, in a review that had just measured this failing twice.
- **2026-08-14 (later)** the items got recorded, but by a PARALLEL session, not the reviewing one.
  That is luck, not a mechanism.
- **2026-08-19** failed inside this skill's own run, on the calendar-cleanup task, where ownership
  was unambiguous.

**Four re-wordings (1.16 prose → 1.18 sequence → 1.19 ownership → 1.23 correct destination) have not
made it fire reliably.** The pattern across this whole skill is consistent and worth more than the
rule itself: **a prose rule only fires if the model happens to re-read it at the moment of the
action. Rules that must survive 40 turns need a MECHANISM that produces output.** The date rule got
one (a `PreToolUse` hook on git commit) and stopped failing. This one has not.

**Next session that runs this skill: do not write a 5th wording.** Check `~/.claude/settings.json`
and report whether a hook can verify a tracker write happened before a skill-trainer closing summary
is allowed. That is the open task.

> ✅ **CHECKED 2026-08-23. The answer is YES, it is feasible, and here is the finding.**
> `settings.json` already runs a `Stop` hook (`qc/stop-gate.sh`) that **blocks the turn from ending**
> and has been catching QC failures reliably all session. A tracker gate would be the same shape: a
> `Stop` hook that refuses to end a skill-trainer run until a tracker file was written this session.
> **The hard part is not the hook, it is detecting that a skill-trainer run is in progress** —
> `stop-gate.sh` knows what to check because a `PostToolUse` hook logs every edited file to
> `render-state/edited-<session>.txt`. The same trick works: log skill invocations, then have the
> Stop gate require a write to a `STATE.md`/`NEXT.md`/`TRACKER.md` before letting a review close.
> **Not built on 2026-08-23** — the session built the diagnosis gate instead, which was the higher
> Pareto item that day (11 wrong assertions vs 0 untracked follow-ups; that run's follow-ups all
> reached `STATE.md`). This stays the open task, but it is now scoped rather than open-ended.

## Step 3 — CLARIFY (ask before fixing)
Ask the questions you need answered before revising. **Do not assume** intent, scope, or priority. Lead every choice with a recommendation, marked "(Recommended)", plus a one-line pro/con (the repo's Rule 11 — recommend, don't just ask). Then **STOP and wait**. Do not begin Step 4 until the user answers.

### ⛔ MEASURE FIRST. Never ask the user a question research can answer (added v1.20.0, 2026-08-15, Dee's call)

**Before every question, ask yourself: could a tool answer this in under 10 minutes?** If yes, go get the answer and bring back the numbers. Do not spend the user's turn on it.

**Only these belong in a question to the user:** money, faith, family, risk appetite, what she wants to build, and anything about people she knows personally. Everything else, market size, what competitors sell, what buyers search, whether something is saturated, who owns a domain, how many titles a catalog needs, is **your job to measure**.

**Live case (2026-08-15, GasLamp).** A session asked Dee 4 questions. She answered 3 with **"IDK"** and wrote: *"seems we still hvaen't done the resarch that I'm asking and we're all over the place which is what I wanted help with."* All 3 were researchable. When the session finally opened the Amazon bestseller list, it answered all 3 in about 6 tool calls, and the answers were better than anything she could have guessed. Earlier in the same session she also had to say *"it seems you can check to see who owns the domain?"*, a `whois` away, and *"did you actually recomemnd anything."*

**The tell:** if you are offering the user 3 options and you do not know which is true, you are outsourcing research as a decision. That is not a question, it is a stall.

**Also measure the BAR before you benchmark.** The same session compared everything to category winners (#59 in Books) for hours. Dee's actual target was *"one or two per day, five per day."* Those are different markets and the whole analysis was aimed at the wrong one. **Ask what "good" looks like in numbers at the START of any market question**, then measure against that. This is the one number worth asking for, because only she knows it.

## Step 4 — IMPROVE (only after answers)
Produce the improved version, incorporating every gap the user signed off on. Make the fixes **reproducible and plain enough for a 6th grader**: where things live, what changed, and why. When a lesson is general, write it where it will be reused — a `STATE.md`, an SOP, or the relevant skill — so it sticks and is not relearned next time.

**Before writing into a shared skill file, re-read it fresh and check for conflicts with what you're about to add (2026-07-01).** Dee runs lessons-learned across several threads at once on purpose, and the same skill file can be mid-edit by another thread when you go to write yours. Don't write from the copy you read at the start of the session. Immediately before the edit: re-open the file, read what's already there (including any version bump another thread already made), and check whether it says something that agrees, disagrees, or overlaps with the lesson you're about to add. If it conflicts, surface that to Dee rather than silently picking a side or silently overwriting. If it's just a numbering collision with no real content conflict, bump past whichever version is highest and note the collision inline (see the article-engine 1.9.0/1.10.0 entries for the pattern). Dee's own words: "when things are being overwritten, go back and take a look at what's already written there and see if there's any conflicts with what you're about to write... we need the last one to double-check."

**If IMPROVE edits a shared skill file with raw `sed`/bash text-replace instead of the Edit tool, that edit skips the staleness check the Edit tool would have enforced (added 2026-08-09).** Bash-level text replace happens to work even when the file has moved on since you last read it — it doesn't fail loudly like Edit would on a stale match. So after any `sed`/bash edit to a shared file, manually re-read the full result and verify: the version/changelog history has no duplicate or garbled entries, your new content sits intact and correctly placed, and nothing from a parallel thread's edit got clobbered. Prefer the Edit tool for shared-file writes when practical; when a raw text-replace is the right tool (e.g. a bulk find-and-replace across many files, like a date-stamp fix), treat the post-edit read as mandatory, not optional.

## ✅ Done when (acceptance criteria)
- [ ] All four steps done, **in order**, none skipped or merged.
- [ ] EVALUATE cited the **real thread** (feedback, corrections, what was approved), not memory, and verified checkable facts with tools.
- [ ] The **process checklist** was walked and its findings reported.
- [ ] GAPS were **ranked** and unsoftened; CLARIFY asked real questions, led with recommendations, and **stopped** for answers.
- [ ] IMPROVE happened **only after** the user answered, and general lessons were written somewhere reusable.

## Output shape (keep it scannable)
Use these headers exactly, in this order: **EVALUATE** → **GAPS** (including the checklist findings) → **CLARIFY** (the questions, then stop) → and, after answers, **IMPROVE**.

## Plain-English crosswalk (why these steps)
- EVALUATE + measure-first = ISO "check against the real thing," Six Sigma "Measure."
- GAPS + ranked checklist = Six Sigma "Analyze" + Pareto (fix the vital few).
- CLARIFY before fixing = mistake-proofing (poka-yoke): you cannot fix the wrong thing if you ask first.
- IMPROVE + write-the-lesson-down = Lean "kaizen": small logged improvements that compound.

## Works with
- **Governed by** `SKILL-AUTHORING-STANDARD.md` (how skills are written) and `OUTPUT-QA-AND-DELIVERY-STANDARD.md` (the visual ship gate). This skill RUNS the review and folds approved lessons back into whichever skill was used.
- **Not** `skill-creator` (which builds NEW skills) or `code-review` (code correctness). This trains existing skills and the way of working.
- **Pairs with** any skill that was just used; that is the one it grades and improves.
